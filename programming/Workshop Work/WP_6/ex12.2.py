import numpy
import matplotlib.pyplot as pyplot

def plotfn(x, y, xlabel, ylabel,):
    #The function which is being used
    #y = (2x^2) + 3x + 4

    #Plotting function
    pyplot.plot(x,y)
    pyplot.xlabel(xlabel)
    pyplot.ylabel(ylabel)
    pyplot.title(title)

    pyplot.show()

    return plotfn


if __name__ == '__main__':

    import ex12.2
    import numpy

    ex12.2.plotfn(linspace(0,9,10),(2x^2) + 3x + 4,test x, testy, lel)
